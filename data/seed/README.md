# Seed data

`data/exercises/exercises.json` is the reviewable source of truth for the initial exercise catalog. Run `python scripts/seed_data.py` from the repository root after installing backend dependencies to initialize the local SQLite database.

The catalog is intentionally conservative. ZERO mode does not pretend to provide a full pulling pattern without an anchor or bar.
